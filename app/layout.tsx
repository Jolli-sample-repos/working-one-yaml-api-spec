import { Footer, Layout, Navbar } from 'nextra-theme-docs'
import { Head } from 'nextra/components'
import { getPageMap } from 'nextra/page-map'
import 'nextra-theme-docs/style.css'

export const metadata = {
  title: {
    default: 'Sample One API Spec - YAML',
    template: '%s – Docs'
  },
  description: 'Documentation site'
}

const navbar = (
  <Navbar
    logo={<span style={{ fontWeight: 700 }}>Sample One API Spec - YAML</span>}
    projectLink="https://github.com/Jolli-sample-repos/working-one-yaml-api-spec"
  />
)

const footer = <Footer>Sample One API Spec - YAML · Generated by Jolli</Footer>

export default async function RootLayout({
  children
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" dir="ltr" suppressHydrationWarning>
      <Head>
        <style>{`:root { --nextra-primary-hue: 212deg; }`}</style>
        
      </Head>
      <body>
        <Layout
          navbar={navbar}
          footer={footer}
          pageMap={await getPageMap()}
          editLink={null}
          feedback={{ content: null }}
          toc={{ title: 'On This Page' }}
          darkMode={true}
          nextThemes={{ defaultTheme: 'system' }}
        >
          {children}
        </Layout>
      </body>
    </html>
  )
}
